# Nixpacks configuration for Railway deployment
# This runs from REPO ROOT — all commands must cd into backend/

providers = []

[phases.setup]
nixPkgs = ["php83", "php83Packages.composer", "php83Extensions.pdo", "php83Extensions.pdo_pgsql", "php83Extensions.pgsql", "php83Extensions.mbstring", "php83Extensions.openssl", "php83Extensions.curl", "php83Extensions.fileinfo", "php83Extensions.tokenizer", "php83Extensions.xml", "php83Extensions.zip"]

[phases.install]
cmds = ["if [ -d 'backend' ]; then cd backend; fi && composer install --no-dev --optimize-autoloader --no-interaction"]

[phases.build]
cmds = [
    "if [ -d 'backend' ]; then cd backend; fi && mkdir -p storage/framework/{sessions,views,cache}",
    "if [ -d 'backend' ]; then cd backend; fi && mkdir -p storage/logs",
    "if [ -d 'backend' ]; then cd backend; fi && mkdir -p bootstrap/cache"
]

[start]
cmd = "if [ -d 'backend' ]; then cd backend; fi && bash start.sh"
